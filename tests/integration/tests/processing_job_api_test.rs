//! ProcessingJob API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the ProcessingJob CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PROCESSINGJOB TEST DATA GENERATOR
// ============================================================================

/// Test data generator for ProcessingJob
pub struct ProcessingJobTestData;

impl TestDataGenerator for ProcessingJobTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "file_id": null,
            "job_type": null,
            "status": null,
            "priority": null,
            "input_data": null,
            "result_data": null,
            "error_message": null,
            "started_at": now,
            "completed_at": now,
            "retry_count": null,
            "max_retries": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "file_id": null,
            "job_type": null,
            "status": null,
            "priority": null,
            "input_data": null,
            "result_data": null,
            "error_message": null,
            "started_at": now,
            "completed_at": now,
            "retry_count": null,
            "max_retries": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PROCESSINGJOB API TEST
// ============================================================================

/// ProcessingJob API test suite
pub struct ProcessingJobApiTest {
    inner: GenericCrudTest<ProcessingJobTestData>,
}

impl ProcessingJobApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/processing_jobs", "ProcessingJob");
        Self {
            inner: GenericCrudTest::new(config, ProcessingJobTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for ProcessingJobApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_processing_job_crud() {
        let mut test = ProcessingJobApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add ProcessingJob specific tests here
// <<< CUSTOM TESTS END >>>
