//! ContentHash API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the ContentHash CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// CONTENTHASH TEST DATA GENERATOR
// ============================================================================

/// Test data generator for ContentHash
pub struct ContentHashTestData;

impl TestDataGenerator for ContentHashTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "hash": null,
            "size_bytes": null,
            "storage_key": null,
            "storage_backend": null,
            "reference_count": null,
            "first_uploaded_at": now,
            "last_referenced_at": now,
            "fingerprint": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "hash": null,
            "size_bytes": null,
            "storage_key": null,
            "storage_backend": null,
            "reference_count": null,
            "first_uploaded_at": now,
            "last_referenced_at": now,
            "fingerprint": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// CONTENTHASH API TEST
// ============================================================================

/// ContentHash API test suite
pub struct ContentHashApiTest {
    inner: GenericCrudTest<ContentHashTestData>,
}

impl ContentHashApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/content_hashes", "ContentHash");
        Self {
            inner: GenericCrudTest::new(config, ContentHashTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for ContentHashApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_content_hash_crud() {
        let mut test = ContentHashApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add ContentHash specific tests here
// <<< CUSTOM TESTS END >>>
