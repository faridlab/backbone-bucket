//! FileShare API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the FileShare CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// FILESHARE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for FileShare
pub struct FileShareTestData;

impl TestDataGenerator for FileShareTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "file_id": null,
            "owner_id": null,
            "token": null,
            "share_type": null,
            "permission": null,
            "shared_with": null,
            "password_hash": null,
            "max_downloads": null,
            "download_count": null,
            "expires_at": now,
            "share_status": null,
            "is_active": true,
            "revoked_at": now,
            "revoked_by": null,
            "message": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "file_id": null,
            "owner_id": null,
            "token": null,
            "share_type": null,
            "permission": null,
            "shared_with": null,
            "password_hash": null,
            "max_downloads": null,
            "download_count": null,
            "expires_at": now,
            "share_status": null,
            "is_active": true,
            "revoked_at": now,
            "revoked_by": null,
            "message": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// FILESHARE API TEST
// ============================================================================

/// FileShare API test suite
pub struct FileShareApiTest {
    inner: GenericCrudTest<FileShareTestData>,
}

impl FileShareApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/file_shares", "FileShare");
        Self {
            inner: GenericCrudTest::new(config, FileShareTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for FileShareApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_file_share_crud() {
        let mut test = FileShareApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add FileShare specific tests here
// <<< CUSTOM TESTS END >>>
