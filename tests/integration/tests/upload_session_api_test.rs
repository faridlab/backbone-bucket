//! UploadSession API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the UploadSession CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// UPLOADSESSION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for UploadSession
pub struct UploadSessionTestData;

impl TestDataGenerator for UploadSessionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "bucket_id": null,
            "user_id": null,
            "path": null,
            "filename": null,
            "mime_type": null,
            "file_size": null,
            "chunk_size": null,
            "total_chunks": null,
            "uploaded_chunks": null,
            "status": null,
            "storage_backend": null,
            "completed_parts": null,
            "part_etags": null,
            "expires_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "bucket_id": null,
            "user_id": null,
            "path": null,
            "filename": null,
            "mime_type": null,
            "file_size": null,
            "chunk_size": null,
            "total_chunks": null,
            "uploaded_chunks": null,
            "status": null,
            "storage_backend": null,
            "completed_parts": null,
            "part_etags": null,
            "expires_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// UPLOADSESSION API TEST
// ============================================================================

/// UploadSession API test suite
pub struct UploadSessionApiTest {
    inner: GenericCrudTest<UploadSessionTestData>,
}

impl UploadSessionApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/upload_sessions", "UploadSession");
        Self {
            inner: GenericCrudTest::new(config, UploadSessionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for UploadSessionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_upload_session_crud() {
        let mut test = UploadSessionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add UploadSession specific tests here
// <<< CUSTOM TESTS END >>>
