//! ConversionJob API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the ConversionJob CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// CONVERSIONJOB TEST DATA GENERATOR
// ============================================================================

/// Test data generator for ConversionJob
pub struct ConversionJobTestData;

impl TestDataGenerator for ConversionJobTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "source_file_id": null,
            "target_format": null,
            "status": null,
            "conversion_options": null,
            "result_file_id": null,
            "progress": null,
            "error_message": null,
            "started_at": now,
            "completed_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "source_file_id": null,
            "target_format": null,
            "status": null,
            "conversion_options": null,
            "result_file_id": null,
            "progress": null,
            "error_message": null,
            "started_at": now,
            "completed_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// CONVERSIONJOB API TEST
// ============================================================================

/// ConversionJob API test suite
pub struct ConversionJobApiTest {
    inner: GenericCrudTest<ConversionJobTestData>,
}

impl ConversionJobApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/conversion_jobs", "ConversionJob");
        Self {
            inner: GenericCrudTest::new(config, ConversionJobTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for ConversionJobApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_conversion_job_crud() {
        let mut test = ConversionJobApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add ConversionJob specific tests here
// <<< CUSTOM TESTS END >>>
