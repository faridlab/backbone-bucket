//! StoredFile GraphQL resolvers
//!
//! Auto-generated by backbone-schema. Do not edit manually.

use std::sync::Arc;
use async_graphql::*;

use crate::application::service::StoredFileService;
use crate::domain::entity::*;

// =================================================================
// GraphQL Output Type
// =================================================================

/// StoredFile GraphQL output type
#[derive(Debug, Clone, SimpleObject)]
#[graphql(name = "BucketStoredFile")]
pub struct StoredFileGql {
    pub id: String,
    pub bucket_id: String,
    pub owner_id: String,
    pub path: String,
    pub original_name: String,
    pub size_bytes: i64,
    pub mime_type: String,
    pub checksum: Option<String>,
    pub is_compressed: bool,
    pub original_size: Option<i64>,
    pub compression_algorithm: Option<String>,
    pub is_scanned: bool,
    pub scan_result: Option<async_graphql::Json<serde_json::Value>>,
    pub threat_level: Option<String>,
    pub has_thumbnail: bool,
    pub thumbnail_path: Option<String>,
    pub has_video_thumbnail: bool,
    pub has_document_preview: bool,
    pub processing_status: Option<String>,
    pub content_hash_id: Option<String>,
    pub cdn_url: Option<String>,
    pub cdn_url_expires_at: Option<String>,
    pub status: String,
    pub storage_key: String,
    pub version: i32,
    pub previous_version_id: Option<String>,
    pub download_count: i32,
    pub last_accessed_at: Option<String>,
    pub metadata: async_graphql::Json<serde_json::Value>,
}

impl From<StoredFile> for StoredFileGql {
    fn from(e: StoredFile) -> Self {
        Self {
            id: e.id.to_string(),
            bucket_id: e.bucket_id.to_string(),
            owner_id: e.owner_id.to_string(),
            path: e.path,
            original_name: e.original_name,
            size_bytes: e.size_bytes,
            mime_type: e.mime_type,
            checksum: e.checksum,
            is_compressed: e.is_compressed,
            original_size: e.original_size,
            compression_algorithm: e.compression_algorithm,
            is_scanned: e.is_scanned,
            scan_result: e.scan_result.map(|v| async_graphql::Json(v)),
            threat_level: e.threat_level.map(|v| v.to_string()),
            has_thumbnail: e.has_thumbnail,
            thumbnail_path: e.thumbnail_path,
            has_video_thumbnail: e.has_video_thumbnail,
            has_document_preview: e.has_document_preview,
            processing_status: e.processing_status.map(|v| v.to_string()),
            content_hash_id: e.content_hash_id.map(|v| v.to_string()),
            cdn_url: e.cdn_url,
            cdn_url_expires_at: e.cdn_url_expires_at.map(|v| v.to_rfc3339()),
            status: e.status.to_string(),
            storage_key: e.storage_key,
            version: e.version,
            previous_version_id: e.previous_version_id.map(|v| v.to_string()),
            download_count: e.download_count,
            last_accessed_at: e.last_accessed_at.map(|v| v.to_rfc3339()),
            metadata: async_graphql::Json(e.metadata),
        }
    }
}

// =================================================================
// Paginated Response
// =================================================================

#[derive(Debug, SimpleObject)]
#[graphql(name = "BucketStoredFilePaginatedResponse")]
pub struct StoredFilePaginatedResponse {
    pub data: Vec<StoredFileGql>,
    pub total: i64,
    pub page: i32,
    pub limit: i32,
    pub total_pages: i32,
}

// =================================================================
// Input Types
// =================================================================

/// Input for creating a StoredFile
#[derive(Debug, InputObject)]
#[graphql(name = "BucketCreateStoredFileInput")]
pub struct CreateStoredFileInput {
    pub bucket_id: String,
    pub owner_id: String,
    pub path: String,
    pub original_name: String,
    pub size_bytes: i64,
    pub mime_type: String,
    pub checksum: Option<String>,
    pub is_compressed: bool,
    pub original_size: Option<i64>,
    pub compression_algorithm: Option<String>,
    pub is_scanned: bool,
    pub scan_result: Option<async_graphql::Json<serde_json::Value>>,
    pub threat_level: Option<String>,
    pub has_thumbnail: bool,
    pub thumbnail_path: Option<String>,
    pub has_video_thumbnail: bool,
    pub has_document_preview: bool,
    pub processing_status: Option<String>,
    pub content_hash_id: Option<String>,
    pub cdn_url: Option<String>,
    pub cdn_url_expires_at: Option<String>,
    pub status: String,
    pub storage_key: String,
    pub version: i32,
    pub previous_version_id: Option<String>,
    pub download_count: i32,
    pub last_accessed_at: Option<String>,
}

/// Input for updating a StoredFile
///
/// All fields are optional. Only provided fields will be updated.
#[derive(Debug, InputObject)]
#[graphql(name = "BucketUpdateStoredFileInput")]
pub struct UpdateStoredFileInput {
    pub bucket_id: Option<String>,
    pub owner_id: Option<String>,
    pub path: Option<String>,
    pub original_name: Option<String>,
    pub size_bytes: Option<i64>,
    pub mime_type: Option<String>,
    pub checksum: Option<Option<String>>,
    pub is_compressed: Option<bool>,
    pub original_size: Option<Option<i64>>,
    pub compression_algorithm: Option<Option<String>>,
    pub is_scanned: Option<bool>,
    pub scan_result: Option<Option<async_graphql::Json<serde_json::Value>>>,
    pub threat_level: Option<Option<String>>,
    pub has_thumbnail: Option<bool>,
    pub thumbnail_path: Option<Option<String>>,
    pub has_video_thumbnail: Option<bool>,
    pub has_document_preview: Option<bool>,
    pub processing_status: Option<Option<String>>,
    pub content_hash_id: Option<Option<String>>,
    pub cdn_url: Option<Option<String>>,
    pub cdn_url_expires_at: Option<Option<String>>,
    pub status: Option<String>,
    pub storage_key: Option<String>,
    pub version: Option<i32>,
    pub previous_version_id: Option<Option<String>>,
    pub download_count: Option<i32>,
    pub last_accessed_at: Option<Option<String>>,
}

// =================================================================
// Query Resolvers
// =================================================================

#[derive(Default)]
pub struct StoredFileQuery;

#[Object(name = "BucketStoredFileQuery")]
impl StoredFileQuery {
    /// List stored_files with pagination
    async fn bucket_stored_files(
        &self,
        ctx: &Context<'_>,
        #[graphql(default = 1)] page: i32,
        #[graphql(default = 20)] limit: i32,
    ) -> Result<StoredFilePaginatedResponse> {
        let service = ctx.data::<Arc<StoredFileService>>()?;
        let p = page.max(1) as u32;
        let l = limit.clamp(1, 100) as u32;
        let (items, total) = service.list(p, l, Default::default()).await
            .map_err(|e| Error::new(e.to_string()))?;

        let total_pages = if l == 0 { 0 } else { ((total as f64) / (l as f64)).ceil() as i32 };

        Ok(StoredFilePaginatedResponse {
            data: items.into_iter().map(Into::into).collect(),
            total: total as i64,
            page: p as i32,
            limit: l as i32,
            total_pages,
        })
    }

    /// Get stored_file by ID
    async fn bucket_stored_file(&self, ctx: &Context<'_>, id: String) -> Result<Option<StoredFileGql>> {
        let service = ctx.data::<Arc<StoredFileService>>()?;
        let entity = service.find_by_id(&id).await
            .map_err(|e| Error::new(e.to_string()))?;
        Ok(entity.map(Into::into))
    }
}

// =================================================================
// Mutation Resolvers
// =================================================================

#[derive(Default)]
pub struct StoredFileMutation;

#[Object(name = "BucketStoredFileMutation")]
impl StoredFileMutation {
    /// Create a new stored_file
    async fn bucket_create_stored_file(
        &self,
        ctx: &Context<'_>,
        input: CreateStoredFileInput,
    ) -> Result<StoredFileGql> {
        let service = ctx.data::<Arc<StoredFileService>>()?;

        let entity = StoredFile {
            id: uuid::Uuid::new_v4(),
            bucket_id: input.bucket_id.parse::<uuid::Uuid>()
                .map_err(|e| Error::new(format!("Invalid bucket_id: {}", e)))?,
            owner_id: input.owner_id.parse::<uuid::Uuid>()
                .map_err(|e| Error::new(format!("Invalid owner_id: {}", e)))?,
            path: input.path,
            original_name: input.original_name,
            size_bytes: input.size_bytes,
            mime_type: input.mime_type,
            checksum: input.checksum,
            is_compressed: input.is_compressed,
            original_size: input.original_size,
            compression_algorithm: input.compression_algorithm,
            is_scanned: input.is_scanned,
            scan_result: input.scan_result.map(|v| v.0),
            threat_level: input.threat_level.map(|v| v.parse::<ThreatLevel>()).transpose()
                .map_err(|e| Error::new(format!("Invalid threat_level: {}", e)))?,
            has_thumbnail: input.has_thumbnail,
            thumbnail_path: input.thumbnail_path,
            has_video_thumbnail: input.has_video_thumbnail,
            has_document_preview: input.has_document_preview,
            processing_status: input.processing_status.map(|v| v.parse::<ProcessingStatus>()).transpose()
                .map_err(|e| Error::new(format!("Invalid processing_status: {}", e)))?,
            content_hash_id: input.content_hash_id.map(|v| v.parse::<uuid::Uuid>()).transpose()
                .map_err(|e| Error::new(format!("Invalid content_hash_id: {}", e)))?,
            cdn_url: input.cdn_url,
            cdn_url_expires_at: input.cdn_url_expires_at.map(|v| v.parse::<chrono::DateTime<chrono::Utc>>()).transpose()
                .map_err(|e| Error::new(format!("Invalid cdn_url_expires_at: {}", e)))?,
            status: input.status.parse::<FileStatus>()
                .map_err(|e| Error::new(format!("Invalid status: {}", e)))?,
            storage_key: input.storage_key,
            version: input.version,
            previous_version_id: input.previous_version_id.map(|v| v.parse::<uuid::Uuid>()).transpose()
                .map_err(|e| Error::new(format!("Invalid previous_version_id: {}", e)))?,
            download_count: input.download_count,
            last_accessed_at: input.last_accessed_at.map(|v| v.parse::<chrono::DateTime<chrono::Utc>>()).transpose()
                .map_err(|e| Error::new(format!("Invalid last_accessed_at: {}", e)))?,
            metadata: Default::default(),
        };

        let created = service.create(entity).await
            .map_err(|e| Error::new(e.to_string()))?;
        Ok(created.into())
    }

    /// Update stored_file by ID
    async fn bucket_update_stored_file(
        &self,
        ctx: &Context<'_>,
        id: String,
        input: UpdateStoredFileInput,
    ) -> Result<StoredFileGql> {
        let service = ctx.data::<Arc<StoredFileService>>()?;
        let mut entity = service.find_by_id(&id).await
            .map_err(|e| Error::new(e.to_string()))?
            .ok_or_else(|| Error::new(format!("StoredFile not found: {}", id)))?;

        if let Some(val) = input.bucket_id { entity.bucket_id = val.parse::<uuid::Uuid>()
            .map_err(|e| Error::new(format!("Invalid bucket_id: {}", e)))?; }
        if let Some(val) = input.owner_id { entity.owner_id = val.parse::<uuid::Uuid>()
            .map_err(|e| Error::new(format!("Invalid owner_id: {}", e)))?; }
        if let Some(val) = input.path { entity.path = val; }
        if let Some(val) = input.original_name { entity.original_name = val; }
        if let Some(val) = input.size_bytes { entity.size_bytes = val; }
        if let Some(val) = input.mime_type { entity.mime_type = val; }
        if let Some(val) = input.checksum { entity.checksum = val; }
        if let Some(val) = input.is_compressed { entity.is_compressed = val; }
        if let Some(val) = input.original_size { entity.original_size = val; }
        if let Some(val) = input.compression_algorithm { entity.compression_algorithm = val; }
        if let Some(val) = input.is_scanned { entity.is_scanned = val; }
        if let Some(val) = input.scan_result { entity.scan_result = val.map(|v| v.0); }
        if let Some(val) = input.threat_level { entity.threat_level = val.map(|v| v.parse::<ThreatLevel>()).transpose()
            .map_err(|e| Error::new(format!("Invalid threat_level: {}", e)))?; }
        if let Some(val) = input.has_thumbnail { entity.has_thumbnail = val; }
        if let Some(val) = input.thumbnail_path { entity.thumbnail_path = val; }
        if let Some(val) = input.has_video_thumbnail { entity.has_video_thumbnail = val; }
        if let Some(val) = input.has_document_preview { entity.has_document_preview = val; }
        if let Some(val) = input.processing_status { entity.processing_status = val.map(|v| v.parse::<ProcessingStatus>()).transpose()
            .map_err(|e| Error::new(format!("Invalid processing_status: {}", e)))?; }
        if let Some(val) = input.content_hash_id { entity.content_hash_id = val.map(|v| v.parse::<uuid::Uuid>()).transpose()
            .map_err(|e| Error::new(format!("Invalid content_hash_id: {}", e)))?; }
        if let Some(val) = input.cdn_url { entity.cdn_url = val; }
        if let Some(val) = input.cdn_url_expires_at { entity.cdn_url_expires_at = val.map(|v| v.parse::<chrono::DateTime<chrono::Utc>>()).transpose()
            .map_err(|e| Error::new(format!("Invalid cdn_url_expires_at: {}", e)))?; }
        if let Some(val) = input.status { entity.status = val.parse::<FileStatus>()
            .map_err(|e| Error::new(format!("Invalid status: {}", e)))?; }
        if let Some(val) = input.storage_key { entity.storage_key = val; }
        if let Some(val) = input.version { entity.version = val; }
        if let Some(val) = input.previous_version_id { entity.previous_version_id = val.map(|v| v.parse::<uuid::Uuid>()).transpose()
            .map_err(|e| Error::new(format!("Invalid previous_version_id: {}", e)))?; }
        if let Some(val) = input.download_count { entity.download_count = val; }
        if let Some(val) = input.last_accessed_at { entity.last_accessed_at = val.map(|v| v.parse::<chrono::DateTime<chrono::Utc>>()).transpose()
            .map_err(|e| Error::new(format!("Invalid last_accessed_at: {}", e)))?; }

        let updated = service.update(&id, entity).await
            .map_err(|e| Error::new(e.to_string()))?
            .ok_or_else(|| Error::new(format!("StoredFile not found: {}", id)))?;
        Ok(updated.into())
    }

    /// Delete stored_file (soft delete)
    async fn bucket_delete_stored_file(&self, ctx: &Context<'_>, id: String) -> Result<bool> {
        let service = ctx.data::<Arc<StoredFileService>>()?;
        service.delete(&id).await
            .map_err(|e| Error::new(e.to_string()))
    }
}
