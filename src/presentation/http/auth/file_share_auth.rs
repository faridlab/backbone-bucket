//! Auth configuration for FileShare handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for FileShare operations
pub mod file_shares {
    /// Permission to list file_share entities
    pub const LIST: &str = "file_share:list";
    /// Permission to read file_share entity details
    pub const READ: &str = "file_share:read";
    /// Permission to create file_share entities
    pub const CREATE: &str = "file_share:create";
    /// Permission to update file_share entities
    pub const UPDATE: &str = "file_share:update";
    /// Permission to delete file_share entities
    pub const DELETE: &str = "file_share:delete";
    /// Permission to restore deleted file_share entities
    pub const RESTORE: &str = "file_share:restore";
    /// Permission to view file_share trash
    pub const TRASH: &str = "file_share:trash";
    /// Permission to empty file_share trash (permanent delete)
    pub const EMPTY_TRASH: &str = "file_share:empty_trash";
    /// Permission to bulk create file_share entities
    pub const BULK_CREATE: &str = "file_share:bulk_create";
    /// Permission to upsert file_share entities
    pub const UPSERT: &str = "file_share:upsert";

    // State transition permissions
    /// Permission to execute expire transition
    pub const TRANSITION_EXPIRE: &str = "file_share:transition:expire";
    /// Permission to execute exhaust transition
    pub const TRANSITION_EXHAUST: &str = "file_share:transition:exhaust";
    /// Permission to execute revoke transition
    pub const TRANSITION_REVOKE: &str = "file_share:transition:revoke";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_EXPIRE,
            TRANSITION_EXHAUST,
            TRANSITION_REVOKE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// FileShare Specific Guards
// ==========================================================================

/// Guard that requires list permission for file_share
pub async fn require_list_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for file_share
pub async fn require_read_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for file_share
pub async fn require_create_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for file_share
pub async fn require_update_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for file_share
pub async fn require_delete_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for file_share
pub async fn require_restore_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for file_share
pub async fn require_trash_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for file_share
pub async fn require_empty_trash_file_share(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, file_shares::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

