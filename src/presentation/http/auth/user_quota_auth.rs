//! Auth configuration for UserQuota handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;
use super::AuthContextExt;

use backbone_auth::middleware::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for UserQuota operations
pub mod user_quotas {
    /// Permission to list user_quota entities
    pub const LIST: &str = "user_quota:list";
    /// Permission to read user_quota entity details
    pub const READ: &str = "user_quota:read";
    /// Permission to create user_quota entities
    pub const CREATE: &str = "user_quota:create";
    /// Permission to update user_quota entities
    pub const UPDATE: &str = "user_quota:update";
    /// Permission to delete user_quota entities
    pub const DELETE: &str = "user_quota:delete";
    /// Permission to restore deleted user_quota entities
    pub const RESTORE: &str = "user_quota:restore";
    /// Permission to view user_quota trash
    pub const TRASH: &str = "user_quota:trash";
    /// Permission to empty user_quota trash (permanent delete)
    pub const EMPTY_TRASH: &str = "user_quota:empty_trash";
    /// Permission to bulk create user_quota entities
    pub const BULK_CREATE: &str = "user_quota:bulk_create";
    /// Permission to upsert user_quota entities
    pub const UPSERT: &str = "user_quota:upsert";

    // State transition permissions
    /// Permission to execute exceed transition
    pub const TRANSITION_EXCEED: &str = "user_quota:transition:exceed";
    /// Permission to execute restore transition
    pub const TRANSITION_RESTORE: &str = "user_quota:transition:restore";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_EXCEED,
            TRANSITION_RESTORE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// UserQuota Specific Guards
// ==========================================================================

/// Guard that requires list permission for user_quota
pub async fn require_list_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for user_quota
pub async fn require_read_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for user_quota
pub async fn require_create_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for user_quota
pub async fn require_update_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for user_quota
pub async fn require_delete_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for user_quota
pub async fn require_restore_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for user_quota
pub async fn require_trash_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for user_quota
pub async fn require_empty_trash_user_quota(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_quotas::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

