//! FileAccessLog gRPC service implementation
//!
//! Auto-generated by backbone-cli. Do not edit manually.

use std::sync::Arc;
use tonic::{Request, Response, Status};
use uuid::Uuid;

use crate::domain::entity::FileAccessLog;
use crate::infrastructure::persistence::file_access_log_repository::FileAccessLogRepository;

// Generated protobuf types
use crate::proto::file_access_log_v1::{
    file_access_log_service_server::file_access_logService,
    FileAccessLog as ProtoFileAccessLog,
    ListFileAccessLogRequest, ListFileAccessLogResponse,
    CreateFileAccessLogRequest,
    GetFileAccessLogRequest,
    UpdateFileAccessLogRequest,
    PatchFileAccessLogRequest,
    DeleteFileAccessLogRequest,
    BulkCreateFileAccessLogRequest, BulkCreateFileAccessLogResponse,
    UpsertFileAccessLogRequest,
    ListFileAccessLogTrashRequest,
    RestoreFileAccessLogRequest,
    EmptyFileAccessLogTrashRequest, EmptyFileAccessLogTrashResponse,
    // Streaming types
    StreamFileAccessLogRequest,
    SyncFileAccessLogRequest, SyncFileAccessLogResponse, SyncError,
    WatchFileAccessLogRequest, FileAccessLogEvent,
};

// Streaming imports
use tokio::sync::mpsc;
use tokio_stream::wrappers::ReceiverStream;
use tonic::Streaming;
use futures::StreamExt;

/// FileAccessLog gRPC service implementation
pub struct FileAccessLogGrpcService {
    repository: Arc<FileAccessLogRepository>,
}

impl FileAccessLogGrpcService {
    pub fn new(repository: Arc<FileAccessLogRepository>) -> Self {
        Self { repository }
    }
}

impl From<FileAccessLog> for ProtoFileAccessLog {
    fn from(entity: FileAccessLog) -> Self {
        Self {
            id: entity.id,
            file_id: entity.file_id,
            user_id: entity.user_id,
            share_id: entity.share_id,
            action: entity.action,
            ip_address: entity.ip_address,
            user_agent: entity.user_agent,
            accessed_at: entity.accessed_at,
        }
    }
}

#[tonic::async_trait]
impl file_access_logService for FileAccessLogGrpcService {
    async fn list(
        &self,
        request: Request<ListFileAccessLogRequest>,
    ) -> Result<Response<ListFileAccessLogResponse>, Status> {
        let req = request.into_inner();
        
        let page = if req.page > 0 { req.page } else { 1 };
        let per_page = if req.per_page > 0 { req.per_page } else { 20 };
        let sort_by = if req.sort_by.is_empty() { None } else { Some(req.sort_by.as_str()) };
        let sort_order = if req.sort_order.is_empty() { None } else { Some(req.sort_order.as_str()) };
        
        let (items, total) = self.repository
            .list(page, per_page, sort_by, sort_order)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        let total_pages = ((total as f64) / (per_page as f64)).ceil() as i32;
        
        Ok(Response::new(ListFileAccessLogResponse {
            items: items.into_iter().map(Into::into).collect(),
            total: total as i64,
            page,
            per_page,
            total_pages,
        }))
    }

    async fn create(
        &self,
        request: Request<CreateFileAccessLogRequest>,
    ) -> Result<Response<ProtoFileAccessLog>, Status> {
        let req = request.into_inner();
        
        let entity = FileAccessLog {
            id: Uuid::new_v4().to_string(),
            file_id: req.file_id,
            user_id: req.user_id,
            share_id: req.share_id,
            action: req.action,
            ip_address: req.ip_address,
            user_agent: req.user_agent,
            accessed_at: req.accessed_at,
        };
        
        let created = self.repository
            .create(entity)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(created.into()))
    }

    async fn get(
        &self,
        request: Request<GetFileAccessLogRequest>,
    ) -> Result<Response<ProtoFileAccessLog>, Status> {
        let req = request.into_inner();
        
        let entity = self.repository
            .find_by_id(&req.id)
            .await
            .map_err(|e| Status::internal(e.to_string()))?
            .ok_or_else(|| Status::not_found(format!("FileAccessLog not found: {}", req.id)))?;
        
        Ok(Response::new(entity.into()))
    }

    async fn update(
        &self,
        request: Request<UpdateFileAccessLogRequest>,
    ) -> Result<Response<ProtoFileAccessLog>, Status> {
        let req = request.into_inner();
        
        let existing = self.repository
            .find_by_id(&req.id)
            .await
            .map_err(|e| Status::internal(e.to_string()))?
            .ok_or_else(|| Status::not_found(format!("FileAccessLog not found: {}", req.id)))?;
        
        let entity = FileAccessLog {
            id: existing.id,
            file_id: req.file_id,
            user_id: req.user_id,
            share_id: req.share_id,
            action: req.action,
            ip_address: req.ip_address,
            user_agent: req.user_agent,
            accessed_at: req.accessed_at,
        };
        
        let updated = self.repository
            .update(&req.id, entity)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(updated.into()))
    }

    async fn patch(
        &self,
        request: Request<PatchFileAccessLogRequest>,
    ) -> Result<Response<ProtoFileAccessLog>, Status> {
        let req = request.into_inner();
        
        let mut existing = self.repository
            .find_by_id(&req.id)
            .await
            .map_err(|e| Status::internal(e.to_string()))?
            .ok_or_else(|| Status::not_found(format!("FileAccessLog not found: {}", req.id)))?;
        
        if let Some(value) = req.file_id {
            existing.file_id = value;
        }
        if let Some(value) = req.user_id {
            existing.user_id = value;
        }
        if let Some(value) = req.share_id {
            existing.share_id = value;
        }
        if let Some(value) = req.action {
            existing.action = value;
        }
        if let Some(value) = req.ip_address {
            existing.ip_address = value;
        }
        if let Some(value) = req.user_agent {
            existing.user_agent = value;
        }
        if let Some(value) = req.accessed_at {
            existing.accessed_at = value;
        }
        existing.updated_at = chrono::Utc::now();
        
        let updated = self.repository
            .update(&req.id, existing)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(updated.into()))
    }

    async fn delete(
        &self,
        request: Request<DeleteFileAccessLogRequest>,
    ) -> Result<Response<()>, Status> {
        let req = request.into_inner();
        
        self.repository
            .soft_delete(&req.id)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(()))
    }

    async fn bulk_create(
        &self,
        request: Request<BulkCreateFileAccessLogRequest>,
    ) -> Result<Response<BulkCreateFileAccessLogResponse>, Status> {
        let req = request.into_inner();
        
        let entities: Vec<FileAccessLog> = req.items
            .into_iter()
            .map(|item| FileAccessLog {
                id: Uuid::new_v4().to_string(),
                file_id: item.file_id,
                user_id: item.user_id,
                share_id: item.share_id,
                action: item.action,
                ip_address: item.ip_address,
                user_agent: item.user_agent,
                accessed_at: item.accessed_at,
            })
            .collect();
        
        let count = entities.len() as i32;
        let created = self.repository
            .bulk_create(entities)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(BulkCreateFileAccessLogResponse {
            items: created.into_iter().map(Into::into).collect(),
            created_count: count,
        }))
    }

    async fn upsert(
        &self,
        request: Request<UpsertFileAccessLogRequest>,
    ) -> Result<Response<ProtoFileAccessLog>, Status> {
        let req = request.into_inner();
        
        let key_field = if req.key_field.is_empty() { "id" } else { &req.key_field };
        let existing = self.repository
            .find_by_field(key_field, &req.key_value)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        let entity = match existing {
            Some(mut e) => {
                e.file_id = req.file_id;
                e.user_id = req.user_id;
                e.share_id = req.share_id;
                e.action = req.action;
                e.ip_address = req.ip_address;
                e.user_agent = req.user_agent;
                e.accessed_at = req.accessed_at;
                e
            }
            None => {
                FileAccessLog {
                    id: Uuid::new_v4().to_string(),
                    file_id: req.file_id,
                    user_id: req.user_id,
                    share_id: req.share_id,
                    action: req.action,
                    ip_address: req.ip_address,
                    user_agent: req.user_agent,
                    accessed_at: req.accessed_at,
                }
            }
        };
        
        let result = self.repository
            .upsert(entity)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(result.into()))
    }

    async fn list_trash(
        &self,
        request: Request<ListFileAccessLogTrashRequest>,
    ) -> Result<Response<ListFileAccessLogResponse>, Status> {
        let req = request.into_inner();
        
        let page = if req.page > 0 { req.page } else { 1 };
        let per_page = if req.per_page > 0 { req.per_page } else { 20 };
        
        let (items, total) = self.repository
            .list_deleted(page, per_page)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        let total_pages = ((total as f64) / (per_page as f64)).ceil() as i32;
        
        Ok(Response::new(ListFileAccessLogResponse {
            items: items.into_iter().map(Into::into).collect(),
            total: total as i64,
            page,
            per_page,
            total_pages,
        }))
    }

    async fn restore(
        &self,
        request: Request<RestoreFileAccessLogRequest>,
    ) -> Result<Response<ProtoFileAccessLog>, Status> {
        let req = request.into_inner();
        
        let restored = self.repository
            .restore(&req.id)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(restored.into()))
    }

    async fn empty_trash(
        &self,
        _request: Request<EmptyFileAccessLogTrashRequest>,
    ) -> Result<Response<EmptyFileAccessLogTrashResponse>, Status> {
        let count = self.repository
            .empty_trash()
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(EmptyFileAccessLogTrashResponse {
            deleted_count: count as i64,
        }))
    }
    // ===========================================
    // Streaming RPCs
    // ===========================================

    /// Server streaming: Stream all FileAccessLogs matching criteria
    type StreamListStream = ReceiverStream<Result<ProtoFileAccessLog, Status>>;

    async fn stream_list(
        &self,
        request: Request<StreamFileAccessLogRequest>,
    ) -> Result<Response<Self::StreamListStream>, Status> {
        let req = request.into_inner();
        let (tx, rx) = mpsc::channel(128);
        let repository = self.repository.clone();
        
        tokio::spawn(async move {
            // Get items with streaming support
            let limit = if req.limit > 0 { req.limit as usize } else { usize::MAX };
            let sort_by = if req.sort_by.is_empty() { None } else { Some(req.sort_by.as_str()) };
            let sort_order = if req.sort_order.is_empty() { None } else { Some(req.sort_order.as_str()) };
            
            // Fetch items in batches
            let batch_size = 100;
            let mut page = 1;
            let mut sent = 0usize;
            
            loop {
                match repository.list(page, batch_size, sort_by, sort_order).await {
                    Ok((items, _total)) => {
                        if items.is_empty() {
                            break;
                        }
                        for item in items {
                            if sent >= limit {
                                return;
                            }
                            if tx.send(Ok(item.into())).await.is_err() {
                                return;
                            }
                            sent += 1;
                        }
                        page += 1;
                    }
                    Err(e) => {
                        let _ = tx.send(Err(Status::internal(e.to_string()))).await;
                        return;
                    }
                }
            }
        });
        
        Ok(Response::new(ReceiverStream::new(rx)))
    }

    /// Client streaming: Bulk create FileAccessLogs from stream
    async fn stream_bulk_create(
        &self,
        request: Request<Streaming<CreateFileAccessLogRequest>>,
    ) -> Result<Response<BulkCreateFileAccessLogResponse>, Status> {
        let mut stream = request.into_inner();
        let mut entities = Vec::new();
        
        while let Some(req) = stream.next().await {
            let req = req?;
            let entity = FileAccessLog {
                id: Uuid::new_v4().to_string(),
                file_id: req.file_id,
                user_id: req.user_id,
                share_id: req.share_id,
                action: req.action,
                ip_address: req.ip_address,
                user_agent: req.user_agent,
                accessed_at: req.accessed_at,
            };
            entities.push(entity);
        }
        
        let count = entities.len() as i32;
        let created = self.repository
            .bulk_create(entities)
            .await
            .map_err(|e| Status::internal(e.to_string()))?;
        
        Ok(Response::new(BulkCreateFileAccessLogResponse {
            items: created.into_iter().map(Into::into).collect(),
            created_count: count,
        }))
    }

    /// Bidirectional streaming: Sync FileAccessLogs changes
    type SyncStreamStream = ReceiverStream<Result<SyncFileAccessLogResponse, Status>>;

    async fn sync_stream(
        &self,
        request: Request<Streaming<SyncFileAccessLogRequest>>,
    ) -> Result<Response<Self::SyncStreamStream>, Status> {
        let mut stream = request.into_inner();
        let (tx, rx) = mpsc::channel(128);
        let repository = self.repository.clone();
        
        tokio::spawn(async move {
            while let Some(result) = stream.next().await {
                let req = match result {
                    Ok(r) => r,
                    Err(e) => {
                        let _ = tx.send(Err(Status::internal(e.to_string()))).await;
                        continue;
                    }
                };
                
                let correlation_id = req.correlation_id.clone();
                
                // Process based on operation type
                let response = match req.operation {
                    Some(sync_file_access_log_request::Operation::Create(create_req)) => {
                        // Handle create
                        SyncFileAccessLogResponse {
                            result: None, // TODO: implement create logic
                            correlation_id,
                        }
                    }
                    Some(sync_file_access_log_request::Operation::Update(update_req)) => {
                        // Handle update
                        SyncFileAccessLogResponse {
                            result: None, // TODO: implement update logic
                            correlation_id,
                        }
                    }
                    Some(sync_file_access_log_request::Operation::Delete(delete_req)) => {
                        // Handle delete
                        SyncFileAccessLogResponse {
                            result: None, // TODO: implement delete logic
                            correlation_id,
                        }
                    }
                    None => {
                        SyncFileAccessLogResponse {
                            result: Some(sync_file_access_log_response::Result::Error(SyncError {
                                code: "NO_OPERATION".to_string(),
                                message: "No operation specified".to_string(),
                                details: Default::default(),
                            })),
                            correlation_id,
                        }
                    }
                };
                
                if tx.send(Ok(response)).await.is_err() {
                    break;
                }
            }
        });
        
        Ok(Response::new(ReceiverStream::new(rx)))
    }

    /// Server streaming: Watch for FileAccessLog changes
    type WatchStream = ReceiverStream<Result<FileAccessLogEvent, Status>>;

    async fn watch(
        &self,
        request: Request<WatchFileAccessLogRequest>,
    ) -> Result<Response<Self::WatchStream>, Status> {
        let req = request.into_inner();
        let (tx, rx) = mpsc::channel(128);
        
        // TODO: Integrate with event bus/message broker
        // This is a placeholder that demonstrates the streaming pattern
        tokio::spawn(async move {
            // Keep connection alive - in real implementation
            // this would subscribe to an event bus
            let mut sequence = req.from_sequence.unwrap_or(0);
            
            loop {
                // In real implementation, wait for events from event bus
                tokio::time::sleep(tokio::time::Duration::from_secs(30)).await;
                
                // Send heartbeat or check if channel is still open
                if tx.is_closed() {
                    break;
                }
            }
        });
        
        Ok(Response::new(ReceiverStream::new(rx)))
    }

}

