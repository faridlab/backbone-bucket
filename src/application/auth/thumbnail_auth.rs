//! Auth configuration for Thumbnail handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Thumbnail>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ThumbnailService;
use crate::domain::entity::Thumbnail;

/// Permission string constants for Thumbnail operations.
pub mod permissions {
    pub const LIST:        &str = "thumbnail:list";
    pub const READ:        &str = "thumbnail:read";
    pub const CREATE:      &str = "thumbnail:create";
    pub const UPDATE:      &str = "thumbnail:update";
    pub const DELETE:      &str = "thumbnail:delete";
    pub const RESTORE:     &str = "thumbnail:restore";
    pub const TRASH:       &str = "thumbnail:trash";
    pub const EMPTY_TRASH: &str = "thumbnail:empty_trash";
    pub const BULK_CREATE: &str = "thumbnail:bulk_create";
    pub const UPSERT:      &str = "thumbnail:upsert";
}

/// Resource policy for Thumbnail — maps CRUD actions to permission strings.
pub struct ThumbnailPolicy;

#[async_trait]
impl ResourcePolicy<Thumbnail> for ThumbnailPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Thumbnail,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for Thumbnail handlers.
///
/// Wraps `ThumbnailService` and checks `ThumbnailPolicy` before delegating.
pub type ThumbnailGuard = PermissionGuard<Thumbnail>;

/// Service-integrated permission guard for Thumbnail (for use in generated handlers).
pub type ThumbnailServiceGuard = ServicePermissionGuard<Thumbnail, ThumbnailService, ThumbnailPolicy>;

// <<< CUSTOM
// END CUSTOM
