//! Auth configuration for ProcessingJob handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ProcessingJob>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ProcessingJobService;
use crate::domain::entity::ProcessingJob;

/// Permission string constants for ProcessingJob operations.
pub mod permissions {
    pub const LIST:        &str = "processing_job:list";
    pub const READ:        &str = "processing_job:read";
    pub const CREATE:      &str = "processing_job:create";
    pub const UPDATE:      &str = "processing_job:update";
    pub const DELETE:      &str = "processing_job:delete";
    pub const RESTORE:     &str = "processing_job:restore";
    pub const TRASH:       &str = "processing_job:trash";
    pub const EMPTY_TRASH: &str = "processing_job:empty_trash";
    pub const BULK_CREATE: &str = "processing_job:bulk_create";
    pub const UPSERT:      &str = "processing_job:upsert";
}

/// Resource policy for ProcessingJob — maps CRUD actions to permission strings.
pub struct ProcessingJobPolicy;

#[async_trait]
impl ResourcePolicy<ProcessingJob> for ProcessingJobPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ProcessingJob,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for ProcessingJob handlers.
///
/// Wraps `ProcessingJobService` and checks `ProcessingJobPolicy` before delegating.
pub type ProcessingJobGuard = PermissionGuard<ProcessingJob>;

/// Service-integrated permission guard for ProcessingJob (for use in generated handlers).
pub type ProcessingJobServiceGuard = ServicePermissionGuard<ProcessingJob, ProcessingJobService, ProcessingJobPolicy>;

// <<< CUSTOM
// END CUSTOM
