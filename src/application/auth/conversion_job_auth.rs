//! Auth configuration for ConversionJob handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ConversionJob>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ConversionJobService;
use crate::domain::entity::ConversionJob;

/// Permission string constants for ConversionJob operations.
pub mod permissions {
    pub const LIST:        &str = "conversion_job:list";
    pub const READ:        &str = "conversion_job:read";
    pub const CREATE:      &str = "conversion_job:create";
    pub const UPDATE:      &str = "conversion_job:update";
    pub const DELETE:      &str = "conversion_job:delete";
    pub const RESTORE:     &str = "conversion_job:restore";
    pub const TRASH:       &str = "conversion_job:trash";
    pub const EMPTY_TRASH: &str = "conversion_job:empty_trash";
    pub const BULK_CREATE: &str = "conversion_job:bulk_create";
    pub const UPSERT:      &str = "conversion_job:upsert";
}

/// Resource policy for ConversionJob — maps CRUD actions to permission strings.
pub struct ConversionJobPolicy;

#[async_trait]
impl ResourcePolicy<ConversionJob> for ConversionJobPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ConversionJob,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for ConversionJob handlers.
///
/// Wraps `ConversionJobService` and checks `ConversionJobPolicy` before delegating.
pub type ConversionJobGuard = PermissionGuard<ConversionJob, ConversionJobService, ConversionJobPolicy>;

/// Service-integrated permission guard for ConversionJob (for use in generated handlers).
pub type ConversionJobServiceGuard = ServicePermissionGuard<ConversionJob, ConversionJobService, ConversionJobPolicy>;

// <<< CUSTOM
// END CUSTOM
