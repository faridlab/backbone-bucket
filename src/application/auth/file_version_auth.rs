//! Auth configuration for FileVersion handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<FileVersion>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::FileVersionService;
use crate::domain::entity::FileVersion;

/// Permission string constants for FileVersion operations.
pub mod permissions {
    pub const LIST:        &str = "file_version:list";
    pub const READ:        &str = "file_version:read";
    pub const CREATE:      &str = "file_version:create";
    pub const UPDATE:      &str = "file_version:update";
    pub const DELETE:      &str = "file_version:delete";
    pub const RESTORE:     &str = "file_version:restore";
    pub const TRASH:       &str = "file_version:trash";
    pub const EMPTY_TRASH: &str = "file_version:empty_trash";
    pub const BULK_CREATE: &str = "file_version:bulk_create";
    pub const UPSERT:      &str = "file_version:upsert";
}

/// Resource policy for FileVersion — maps CRUD actions to permission strings.
pub struct FileVersionPolicy;

#[async_trait]
impl ResourcePolicy<FileVersion> for FileVersionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &FileVersion,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for FileVersion handlers.
///
/// Wraps `FileVersionService` and checks `FileVersionPolicy` before delegating.
pub type FileVersionGuard = PermissionGuard<FileVersion>;

/// Service-integrated permission guard for FileVersion (for use in generated handlers).
pub type FileVersionServiceGuard = ServicePermissionGuard<FileVersion, FileVersionService, FileVersionPolicy>;

// <<< CUSTOM
// END CUSTOM
