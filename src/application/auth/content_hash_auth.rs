//! Auth configuration for ContentHash handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ContentHash>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ContentHashService;
use crate::domain::entity::ContentHash;

/// Permission string constants for ContentHash operations.
pub mod permissions {
    pub const LIST:        &str = "content_hash:list";
    pub const READ:        &str = "content_hash:read";
    pub const CREATE:      &str = "content_hash:create";
    pub const UPDATE:      &str = "content_hash:update";
    pub const DELETE:      &str = "content_hash:delete";
    pub const RESTORE:     &str = "content_hash:restore";
    pub const TRASH:       &str = "content_hash:trash";
    pub const EMPTY_TRASH: &str = "content_hash:empty_trash";
    pub const BULK_CREATE: &str = "content_hash:bulk_create";
    pub const UPSERT:      &str = "content_hash:upsert";
}

/// Resource policy for ContentHash — maps CRUD actions to permission strings.
pub struct ContentHashPolicy;

#[async_trait]
impl ResourcePolicy<ContentHash> for ContentHashPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ContentHash,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for ContentHash handlers.
///
/// Wraps `ContentHashService` and checks `ContentHashPolicy` before delegating.
pub type ContentHashGuard = PermissionGuard<ContentHash>;

/// Service-integrated permission guard for ContentHash (for use in generated handlers).
pub type ContentHashServiceGuard = ServicePermissionGuard<ContentHash, ContentHashService, ContentHashPolicy>;

// <<< CUSTOM
// END CUSTOM
