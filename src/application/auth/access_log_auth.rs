//! Auth configuration for AccessLog handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AccessLog>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AccessLogService;
use crate::domain::entity::AccessLog;

/// Permission string constants for AccessLog operations.
pub mod permissions {
    pub const LIST:        &str = "access_log:list";
    pub const READ:        &str = "access_log:read";
    pub const CREATE:      &str = "access_log:create";
    pub const UPDATE:      &str = "access_log:update";
    pub const DELETE:      &str = "access_log:delete";
    pub const RESTORE:     &str = "access_log:restore";
    pub const TRASH:       &str = "access_log:trash";
    pub const EMPTY_TRASH: &str = "access_log:empty_trash";
    pub const BULK_CREATE: &str = "access_log:bulk_create";
    pub const UPSERT:      &str = "access_log:upsert";
}

/// Resource policy for AccessLog — maps CRUD actions to permission strings.
pub struct AccessLogPolicy;

#[async_trait]
impl ResourcePolicy<AccessLog> for AccessLogPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AccessLog,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for AccessLog handlers.
///
/// Wraps `AccessLogService` and checks `AccessLogPolicy` before delegating.
pub type AccessLogGuard = PermissionGuard<AccessLog>;

/// Service-integrated permission guard for AccessLog (for use in generated handlers).
pub type AccessLogServiceGuard = ServicePermissionGuard<AccessLog, AccessLogService, AccessLogPolicy>;

// <<< CUSTOM
// END CUSTOM
