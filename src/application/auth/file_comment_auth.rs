//! Auth configuration for FileComment handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<FileComment>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::FileCommentService;
use crate::domain::entity::FileComment;

/// Permission string constants for FileComment operations.
pub mod permissions {
    pub const LIST:        &str = "file_comment:list";
    pub const READ:        &str = "file_comment:read";
    pub const CREATE:      &str = "file_comment:create";
    pub const UPDATE:      &str = "file_comment:update";
    pub const DELETE:      &str = "file_comment:delete";
    pub const RESTORE:     &str = "file_comment:restore";
    pub const TRASH:       &str = "file_comment:trash";
    pub const EMPTY_TRASH: &str = "file_comment:empty_trash";
    pub const BULK_CREATE: &str = "file_comment:bulk_create";
    pub const UPSERT:      &str = "file_comment:upsert";
}

/// Resource policy for FileComment — maps CRUD actions to permission strings.
pub struct FileCommentPolicy;

#[async_trait]
impl ResourcePolicy<FileComment> for FileCommentPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &FileComment,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for FileComment handlers.
///
/// Wraps `FileCommentService` and checks `FileCommentPolicy` before delegating.
pub type FileCommentGuard = PermissionGuard<FileComment, FileCommentService, FileCommentPolicy>;

/// Service-integrated permission guard for FileComment (for use in generated handlers).
pub type FileCommentServiceGuard = ServicePermissionGuard<FileComment, FileCommentService, FileCommentPolicy>;

// <<< CUSTOM
// END CUSTOM
