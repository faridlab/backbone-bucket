//! Create FileVersion use case
//!
//! Generated by backbone-schema. Do not edit manually.

use std::sync::Arc;

use anyhow::Result;
use async_trait::async_trait;

use super::UseCase;
use crate::domain::entity::FileVersion;
use crate::infrastructure::persistence::FileVersionRepository;

/// Input for creating a FileVersion
#[derive(Debug, Clone)]
pub struct CreateFileVersionInput {
    pub size_bytes: i64,
    pub file_id: Uuid,
    pub version_number: i32,
    pub version_type: VersionType,
    pub storage_key: String,
    pub storage_backend: String,
    pub name: String,
    pub mime_type: String,
    pub checksum_md5: String,
    pub checksum_sha256: String,
    pub created_by_id: Uuid,
    pub change_summary: String,
    pub is_current: bool,
    pub restored_from_id: Uuid,
    pub expires_at: DateTime<Utc>,
    pub is_deleted: bool,
    pub deleted_at: DateTime<Utc>,
}

/// Output after creating a FileVersion
#[derive(Debug, Clone)]
pub struct CreateFileVersionOutput {
    pub file_version: FileVersion,
}

/// Use case for creating a new FileVersion
pub struct CreateFileVersionUseCase {
    repository: Arc<FileVersionRepository>,
}

impl CreateFileVersionUseCase {
    /// Create a new use case instance
    pub fn new(repository: Arc<FileVersionRepository>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl UseCase for CreateFileVersionUseCase {
    type Input = CreateFileVersionInput;
    type Output = CreateFileVersionOutput;

    async fn execute(&self, input: Self::Input) -> Result<Self::Output> {
        // Create entity from input
        let entity = FileVersion {
            created_at: Some(chrono::Utc::now()),
            updated_at: Some(chrono::Utc::now()),
            size_bytes: input.size_bytes,
            id: None,
            file_id: input.file_id,
            version_number: input.version_number,
            version_type: input.version_type,
            storage_key: input.storage_key,
            storage_backend: input.storage_backend,
            name: input.name,
            mime_type: input.mime_type,
            checksum_md5: input.checksum_md5,
            checksum_sha256: input.checksum_sha256,
            created_by_id: input.created_by_id,
            change_summary: input.change_summary,
            is_current: input.is_current,
            restored_from_id: input.restored_from_id,
            expires_at: input.expires_at,
            is_deleted: input.is_deleted,
            deleted_at: input.deleted_at,
        };

        // <<< CUSTOM VALIDATION START >>>
        // Add custom validation logic here
        // <<< CUSTOM VALIDATION END >>>

        // Persist entity
        let created = self.repository.create(entity).await?;

        Ok(CreateFileVersionOutput { file_version: created })
    }
}
