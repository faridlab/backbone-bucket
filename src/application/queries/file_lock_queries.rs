//! Queries for FileLock aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::FileLock;
use crate::infrastructure::persistence::{FileLockRepository, PaginationParams, PaginatedResult, FileLockFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for FileLock operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait FileLockQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a file_lock by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetFileLockByIdQuery {
    pub id: String,
}

/// Handler for GetFileLockByIdQuery
pub struct GetFileLockByIdHandler<R: FileLockRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: FileLockRepository> GetFileLockByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: FileLockRepository + 'static> QueryHandler<GetFileLockByIdQuery> for GetFileLockByIdHandler<R> {
    type Output = Option<FileLock>;

    async fn handle(&self, query: GetFileLockByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list file_lock entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListFileLockQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_file_id: Option<Uuid>,
    pub filter_user_id: Option<Uuid>,
    pub filter_status: Option<LockStatus>,
}

impl Default for ListFileLockQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_file_id: None,
            filter_user_id: None,
            filter_status: None,
        }
    }
}

/// Handler for ListFileLockQuery
pub struct ListFileLockHandler<R: FileLockRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: FileLockRepository> ListFileLockHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: FileLockRepository + 'static> QueryHandler<ListFileLockQuery> for ListFileLockHandler<R> {
    type Output = PaginatedResult<FileLock>;

    async fn handle(&self, query: ListFileLockQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = FileLockFilter {
            file_id: query.filter_file_id.clone(),
            user_id: query.filter_user_id.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find file_lock by file_id
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetFileLockByFileIdQuery {
    pub file_id: Uuid,
}

/// Handler for GetFileLockByFileIdQuery
pub struct GetFileLockByFileIdHandler<R: FileLockRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: FileLockRepository> GetFileLockByFileIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: FileLockRepository + 'static> QueryHandler<GetFileLockByFileIdQuery> for GetFileLockByFileIdHandler<R> {
    type Output = Option<FileLock>;

    async fn handle(&self, query: GetFileLockByFileIdQuery) -> Result<Self::Output> {
        self.repository.find_by_file_id(query.file_id).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveFileLockQuery;
// <<< CUSTOM QUERIES END >>>