//! Queries for UploadSession aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::UploadSession;
use crate::infrastructure::persistence::{UploadSessionRepository, PaginationParams, PaginatedResult, UploadSessionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for UploadSession operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait UploadSessionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a upload_session by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetUploadSessionByIdQuery {
    pub id: String,
}

/// Handler for GetUploadSessionByIdQuery
pub struct GetUploadSessionByIdHandler<R: UploadSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UploadSessionRepository> GetUploadSessionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UploadSessionRepository + 'static> QueryHandler<GetUploadSessionByIdQuery> for GetUploadSessionByIdHandler<R> {
    type Output = Option<UploadSession>;

    async fn handle(&self, query: GetUploadSessionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list upload_session entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListUploadSessionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_bucket_id: Option<Uuid>,
    pub filter_user_id: Option<Uuid>,
    pub filter_path: Option<String>,
    pub filter_filename: Option<String>,
    pub filter_mime_type: Option<String>,
    pub filter_file_size: Option<i64>,
    pub filter_status: Option<UploadStatus>,
    pub filter_storage_backend: Option<StorageBackend>,
}

impl Default for ListUploadSessionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_bucket_id: None,
            filter_user_id: None,
            filter_path: None,
            filter_filename: None,
            filter_mime_type: None,
            filter_file_size: None,
            filter_status: None,
            filter_storage_backend: None,
        }
    }
}

/// Handler for ListUploadSessionQuery
pub struct ListUploadSessionHandler<R: UploadSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UploadSessionRepository> ListUploadSessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UploadSessionRepository + 'static> QueryHandler<ListUploadSessionQuery> for ListUploadSessionHandler<R> {
    type Output = PaginatedResult<UploadSession>;

    async fn handle(&self, query: ListUploadSessionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = UploadSessionFilter {
            bucket_id: query.filter_bucket_id.clone(),
            user_id: query.filter_user_id.clone(),
            path: query.filter_path.clone(),
            filename: query.filter_filename.clone(),
            mime_type: query.filter_mime_type.clone(),
            file_size: query.filter_file_size.clone(),
            status: query.filter_status.clone(),
            storage_backend: query.filter_storage_backend.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveUploadSessionQuery;
// <<< CUSTOM QUERIES END >>>