//! Queries for ConversionJob aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::ConversionJob;
use crate::infrastructure::persistence::{ConversionJobRepository, PaginationParams, PaginatedResult, ConversionJobFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for ConversionJob operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait ConversionJobQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a conversion_job by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetConversionJobByIdQuery {
    pub id: String,
}

/// Handler for GetConversionJobByIdQuery
pub struct GetConversionJobByIdHandler<R: ConversionJobRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ConversionJobRepository> GetConversionJobByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ConversionJobRepository + 'static> QueryHandler<GetConversionJobByIdQuery> for GetConversionJobByIdHandler<R> {
    type Output = Option<ConversionJob>;

    async fn handle(&self, query: GetConversionJobByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list conversion_job entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListConversionJobQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_source_file_id: Option<Uuid>,
    pub filter_target_format: Option<String>,
    pub filter_status: Option<ConversionStatus>,
    pub filter_result_file_id: Option<Uuid>,
    pub filter_error_message: Option<String>,
}

impl Default for ListConversionJobQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_source_file_id: None,
            filter_target_format: None,
            filter_status: None,
            filter_result_file_id: None,
            filter_error_message: None,
        }
    }
}

/// Handler for ListConversionJobQuery
pub struct ListConversionJobHandler<R: ConversionJobRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ConversionJobRepository> ListConversionJobHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ConversionJobRepository + 'static> QueryHandler<ListConversionJobQuery> for ListConversionJobHandler<R> {
    type Output = PaginatedResult<ConversionJob>;

    async fn handle(&self, query: ListConversionJobQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = ConversionJobFilter {
            source_file_id: query.filter_source_file_id.clone(),
            target_format: query.filter_target_format.clone(),
            status: query.filter_status.clone(),
            result_file_id: query.filter_result_file_id.clone(),
            error_message: query.filter_error_message.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveConversionJobQuery;
// <<< CUSTOM QUERIES END >>>