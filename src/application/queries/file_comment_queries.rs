//! Queries for FileComment aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::FileComment;
use crate::infrastructure::persistence::{FileCommentRepository, PaginationParams, PaginatedResult, FileCommentFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for FileComment operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait FileCommentQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a file_comment by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetFileCommentByIdQuery {
    pub id: String,
}

/// Handler for GetFileCommentByIdQuery
pub struct GetFileCommentByIdHandler<R: FileCommentRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: FileCommentRepository> GetFileCommentByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: FileCommentRepository + 'static> QueryHandler<GetFileCommentByIdQuery> for GetFileCommentByIdHandler<R> {
    type Output = Option<FileComment>;

    async fn handle(&self, query: GetFileCommentByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list file_comment entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListFileCommentQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_file_id: Option<Uuid>,
    pub filter_user_id: Option<Uuid>,
    pub filter_parent_id: Option<Uuid>,
    pub filter_content: Option<String>,
    pub filter_resolved: Option<bool>,
    pub filter_resolved_by: Option<Uuid>,
    pub filter_status: Option<CommentStatus>,
}

impl Default for ListFileCommentQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_file_id: None,
            filter_user_id: None,
            filter_parent_id: None,
            filter_content: None,
            filter_resolved: None,
            filter_resolved_by: None,
            filter_status: None,
        }
    }
}

/// Handler for ListFileCommentQuery
pub struct ListFileCommentHandler<R: FileCommentRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: FileCommentRepository> ListFileCommentHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: FileCommentRepository + 'static> QueryHandler<ListFileCommentQuery> for ListFileCommentHandler<R> {
    type Output = PaginatedResult<FileComment>;

    async fn handle(&self, query: ListFileCommentQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = FileCommentFilter {
            file_id: query.filter_file_id.clone(),
            user_id: query.filter_user_id.clone(),
            parent_id: query.filter_parent_id.clone(),
            content: query.filter_content.clone(),
            resolved: query.filter_resolved.clone(),
            resolved_by: query.filter_resolved_by.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveFileCommentQuery;
// <<< CUSTOM QUERIES END >>>