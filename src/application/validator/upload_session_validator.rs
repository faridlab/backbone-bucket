//! Validator for UploadSession entity
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<UploadSession>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, OptionalNotBlank, RequiredString};
use crate::domain::entity::UploadSession;

/// Validator type alias for UploadSession entities.
pub type UploadSessionValidator = EntityValidator<UploadSession>;

/// Build a validator for UploadSession with all schema-defined field rules.
pub fn upload_session_validator() -> UploadSessionValidator {
    EntityValidator::new()
        .rule(RequiredString::new("path", |e: &UploadSession| &e.path))
        .rule(RequiredString::new("filename", |e: &UploadSession| &e.filename))
        .rule(OptionalNotBlank::new("mime_type", |e: &UploadSession| e.mime_type.as_deref()))
        .rule(NonNegative::new("uploaded_chunks", |e: &UploadSession| e.uploaded_chunks as i64))
    // <<< CUSTOM RULES
    // hook rule: bucket_must_exist (implement in CUSTOM zone)
    // hook rule: bucket_must_accept_uploads (implement in CUSTOM zone)
    // hook rule: valid_file_size (implement in CUSTOM zone)
    // hook rule: minimum_chunk_size (implement in CUSTOM zone)
    // hook rule: reasonable_chunk_size (implement in CUSTOM zone)
    // hook rule: valid_chunk_count (implement in CUSTOM zone)
    // hook rule: valid_path (implement in CUSTOM zone)
    // hook rule: valid_expiration (implement in CUSTOM zone)
    // hook rule: reasonable_expiration (implement in CUSTOM zone)
    // hook rule: session_not_expired (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM