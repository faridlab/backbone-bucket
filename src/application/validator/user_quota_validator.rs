//! Validator for UserQuota entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<UserQuota>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, RequiredString, RequiredUuid};
use crate::domain::entity::UserQuota;

/// Validator type alias for UserQuota entities.
pub type UserQuotaValidator = EntityValidator<UserQuota>;

/// Build a validator for UserQuota with all schema-defined field rules.
pub fn user_quota_validator() -> UserQuotaValidator {
    EntityValidator::new()
        .rule(NonNegative::new("limit_bytes", |e: &UserQuota| e.limit_bytes))
        .rule(NonNegative::new("used_bytes", |e: &UserQuota| e.used_bytes))
        .rule(RequiredString::new("tier", |e: &UserQuota| &e.tier))
        .rule(NonNegative::new("peak_usage_bytes", |e: &UserQuota| e.peak_usage_bytes))
    // <<< CUSTOM RULES
    // hook rule: user_exists (implement in CUSTOM zone)
    // hook rule: unique_user_quota (implement in CUSTOM zone)
    // hook rule: valid_tier (implement in CUSTOM zone)
    // hook rule: max_storage_positive (implement in CUSTOM zone)
    // hook rule: max_file_size_positive (implement in CUSTOM zone)
    // hook rule: max_file_size_within_storage (implement in CUSTOM zone)
    // hook rule: downgrade_allowed (implement in CUSTOM zone)
    // hook rule: warning_threshold_valid (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM