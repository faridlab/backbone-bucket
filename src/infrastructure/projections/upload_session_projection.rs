//! UploadSession Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// UploadSession read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct UploadSessionProjection {
    pub id: Uuid,
    pub bucket_id: Uuid,
    pub user_id: Uuid,
    pub path: String,
    pub filename: String,
    pub mime_type: Option<String>,
    pub file_size: i64,
    pub chunk_size: i32,
    pub total_chunks: i32,
    pub uploaded_chunks: i32,
    pub status: UploadStatus,
    pub storage_backend: StorageBackend,
    pub completed_parts: Vec<i32>,
    pub part_etags: Option<serde_json::Value>,
    pub expires_at: DateTime<Utc>,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl UploadSessionProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        bucket_id: Uuid,
        user_id: Uuid,
        path: String,
        filename: String,
        mime_type: Option<String>,
        file_size: i64,
        chunk_size: i32,
        total_chunks: i32,
        uploaded_chunks: i32,
        status: UploadStatus,
        storage_backend: StorageBackend,
        completed_parts: Vec<i32>,
        part_etags: Option<serde_json::Value>,
        expires_at: DateTime<Utc>,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            bucket_id,
            user_id,
            path,
            filename,
            mime_type,
            file_size,
            chunk_size,
            total_chunks,
            uploaded_chunks,
            status,
            storage_backend,
            completed_parts,
            part_etags,
            expires_at,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of UploadSession for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct UploadSessionSummary {
    pub id: Uuid,
    pub status: UploadStatus,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct UploadSessionDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>