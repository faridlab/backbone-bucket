//! Repository for FileShare entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<FileShare, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::FileShare;

/// Table name for FileShare entities
pub const TABLE_NAME: &str = "file_shares";

/// Repository for FileShare entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct FileShareRepository(
    backbone_orm::GenericCrudRepository<FileShare, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for FileShareRepository {
    type Target = backbone_orm::GenericCrudRepository<FileShare, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl FileShareRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl FileShareRepository {
    /// Find file_share by token (excludes soft-deleted)
    pub async fn find_by_token(&self, token: &str) -> Result<Option<FileShare>> {
        self.find_by_text_field("token", token).await
    }

    /// Check if file_share exists by token (excludes soft-deleted)
    pub async fn exists_by_token(&self, token: &str) -> Result<bool> {
        self.exists_by_text_field("token", token).await
    }

}

backbone_core::impl_crud_repository!(FileShareRepository, FileShare, soft_delete);
