//! Repository for ContentHash entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<ContentHash, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::ContentHash;

/// Table name for ContentHash entities
pub const TABLE_NAME: &str = "content_hashes";

/// Repository for ContentHash entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct ContentHashRepository(
    backbone_orm::GenericCrudRepository<ContentHash, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for ContentHashRepository {
    type Target = backbone_orm::GenericCrudRepository<ContentHash, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl ContentHashRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl ContentHashRepository {
    /// Find content_hash by hash (excludes soft-deleted)
    pub async fn find_by_hash(&self, hash: &str) -> Result<Option<ContentHash>> {
        self.find_by_text_field("hash", hash).await
    }

    /// Check if content_hash exists by hash (excludes soft-deleted)
    pub async fn exists_by_hash(&self, hash: &str) -> Result<bool> {
        self.exists_by_text_field("hash", hash).await
    }

}

backbone_core::impl_crud_repository!(ContentHashRepository, ContentHash, soft_delete);
