//! Repository for Thumbnail entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Thumbnail, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Thumbnail;

/// Table name for Thumbnail entities
pub const TABLE_NAME: &str = "thumbnails";

/// Repository for Thumbnail entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct ThumbnailRepository(
    backbone_orm::GenericCrudRepository<Thumbnail, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for ThumbnailRepository {
    type Target = backbone_orm::GenericCrudRepository<Thumbnail, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl ThumbnailRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl ThumbnailRepository {
    /// Find thumbnail by storage_key (excludes soft-deleted)
    pub async fn find_by_storage_key(&self, storage_key: &str) -> Result<Option<Thumbnail>> {
        self.find_by_text_field("storage_key", storage_key).await
    }

    /// Check if thumbnail exists by storage_key (excludes soft-deleted)
    pub async fn exists_by_storage_key(&self, storage_key: &str) -> Result<bool> {
        self.exists_by_text_field("storage_key", storage_key).await
    }

}

backbone_core::impl_crud_repository!(ThumbnailRepository, Thumbnail, soft_delete);
