//! Repository for FileComment entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<FileComment, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::FileComment;

/// Table name for FileComment entities
pub const TABLE_NAME: &str = "file_comments";

/// Repository for FileComment entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct FileCommentRepository(
    backbone_orm::GenericCrudRepository<FileComment, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for FileCommentRepository {
    type Target = backbone_orm::GenericCrudRepository<FileComment, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl FileCommentRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(FileCommentRepository, FileComment, soft_delete);
