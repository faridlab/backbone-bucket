//! Repository for FileVersion entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<FileVersion, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::FileVersion;

/// Table name for FileVersion entities
pub const TABLE_NAME: &str = "file_versions";

/// Repository for FileVersion entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct FileVersionRepository(
    backbone_orm::GenericCrudRepository<FileVersion, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for FileVersionRepository {
    type Target = backbone_orm::GenericCrudRepository<FileVersion, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl FileVersionRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl FileVersionRepository {
    /// Find file_version by storage_key (excludes soft-deleted)
    pub async fn find_by_storage_key(&self, storage_key: &str) -> Result<Option<FileVersion>> {
        self.find_by_text_field("storage_key", storage_key).await
    }

    /// Check if file_version exists by storage_key (excludes soft-deleted)
    pub async fn exists_by_storage_key(&self, storage_key: &str) -> Result<bool> {
        self.exists_by_text_field("storage_key", storage_key).await
    }

}

backbone_core::impl_crud_repository!(FileVersionRepository, FileVersion, soft_delete);
