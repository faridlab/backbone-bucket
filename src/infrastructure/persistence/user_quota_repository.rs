//! Repository for UserQuota entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<UserQuota, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;
use uuid::Uuid;

use crate::domain::entity::UserQuota;

/// Table name for UserQuota entities
pub const TABLE_NAME: &str = "user_quotas";

/// Repository for UserQuota entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct UserQuotaRepository(
    backbone_orm::GenericCrudRepository<UserQuota, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for UserQuotaRepository {
    type Target = backbone_orm::GenericCrudRepository<UserQuota, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl UserQuotaRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl UserQuotaRepository {
    /// Find user_quota by user_id (excludes soft-deleted)
    pub async fn find_by_user_id(&self, user_id: uuid::Uuid) -> Result<Option<UserQuota>> {
        self.find_by_uuid_field("user_id", user_id).await
    }

    /// Check if user_quota exists by user_id (excludes soft-deleted)
    pub async fn exists_by_user_id(&self, user_id: uuid::Uuid) -> Result<bool> {
        self.exists_by_uuid_field("user_id", user_id).await
    }

}

backbone_core::impl_crud_repository!(UserQuotaRepository, UserQuota, soft_delete);
