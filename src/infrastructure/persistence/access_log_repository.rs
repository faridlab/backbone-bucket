//! Repository for AccessLog entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<AccessLog, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::AccessLog;

/// Table name for AccessLog entities
pub const TABLE_NAME: &str = "access_logs";

/// Repository for AccessLog entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct AccessLogRepository(
    backbone_orm::GenericCrudRepository<AccessLog, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for AccessLogRepository {
    type Target = backbone_orm::GenericCrudRepository<AccessLog, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl AccessLogRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(AccessLogRepository, AccessLog, soft_delete);
