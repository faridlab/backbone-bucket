//! Domain service for StoredFile aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This service contains domain business logic for StoredFile operations.
//! Add your custom business rules and domain operations here.

// ============================================================================
// IMPORTS - Add custom imports below
// ============================================================================

use anyhow::{Result, anyhow};

use crate::StoredFile;
// <<< CUSTOM IMPORTS START >>>

// <<< CUSTOM IMPORTS START >>>
// Add your custom imports here
// <<< CUSTOM IMPORTS END >>>

// ============================================================================
// DOMAIN SERVICE
// ============================================================================

/// Domain service for StoredFile business operations
///
/// This service contains:
/// - Business validation rules
/// - Domain operations that span multiple entities
/// - Complex business logic
///
/// Note: This is a stateless service. State should be in entities.
pub struct StoredFileDomainService;

impl StoredFileDomainService {
    /// Create a new domain service instance
    pub fn new() -> Self {
        Self
    }

    // =========================================================================
    // Generated Business Operations
    // =========================================================================

    /// Validate a stored_file entity for business rules
    ///
    /// Override this method to add custom validation logic.
    pub fn validate(&self, entity: &StoredFile) -> Result<()> {
        // Default validation - check required fields
        if entity.path.is_empty() {
            return Err(anyhow!("path is required"));
        }
        if entity.original_name.is_empty() {
            return Err(anyhow!("original_name is required"));
        }
        if entity.mime_type.is_empty() {
            return Err(anyhow!("mime_type is required"));
        }
        if entity.storage_key.is_empty() {
            return Err(anyhow!("storage_key is required"));
        }
        Ok(())
    }

    /// Check if a stored_file can be created
    ///
    /// Add custom business rules that must be satisfied before creation.
    pub fn can_create(&self, entity: &StoredFile) -> Result<bool> {
        self.validate(entity)?;
        Ok(true)
    }

    /// Check if a stored_file can be updated
    pub fn can_update(&self, _current: &StoredFile, _updated: &StoredFile) -> Result<bool> {
        // Add update permission/business logic here
        Ok(true)
    }

    /// Check if a stored_file can be deleted
    pub fn can_delete(&self, _entity: &StoredFile) -> Result<bool> {
        // Add deletion permission/business logic here
        Ok(true)
    }

    // =========================================================================
    // Custom Business Operations
    // =========================================================================
    // <<< CUSTOM OPERATIONS START >>>

    // <<< CUSTOM OPERATIONS START >>>
    // Add your custom domain operations here
    // Example:
    // pub fn calculate_something(&self, entity: &StoredFile) -> Result<i32> {
    //     // Business logic here
    //     Ok(0)
    // }
    // <<< CUSTOM OPERATIONS END >>>
}

impl Default for StoredFileDomainService {
    fn default() -> Self {
        Self::new()
    }
}