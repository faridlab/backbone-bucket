//! Domain service for FileVersion aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This service contains domain business logic for FileVersion operations.
//! Add your custom business rules and domain operations here.

// ============================================================================
// IMPORTS - Add custom imports below
// ============================================================================

use anyhow::{Result, anyhow};

use crate::FileVersion;
// <<< CUSTOM IMPORTS START >>>

// <<< CUSTOM IMPORTS START >>>
// Add your custom imports here
// <<< CUSTOM IMPORTS END >>>

// ============================================================================
// DOMAIN SERVICE
// ============================================================================

/// Domain service for FileVersion business operations
///
/// This service contains:
/// - Business validation rules
/// - Domain operations that span multiple entities
/// - Complex business logic
///
/// Note: This is a stateless service. State should be in entities.
pub struct FileVersionDomainService;

impl FileVersionDomainService {
    /// Create a new domain service instance
    pub fn new() -> Self {
        Self
    }

    // =========================================================================
    // Generated Business Operations
    // =========================================================================

    /// Validate a file_version entity for business rules
    ///
    /// Override this method to add custom validation logic.
    pub fn validate(&self, entity: &FileVersion) -> Result<()> {
        // Default validation - check required fields
        if entity.storage_key.is_empty() {
            return Err(anyhow!("storage_key is required"));
        }
        if entity.storage_backend.is_empty() {
            return Err(anyhow!("storage_backend is required"));
        }
        if entity.name.is_empty() {
            return Err(anyhow!("name is required"));
        }
        if entity.mime_type.is_empty() {
            return Err(anyhow!("mime_type is required"));
        }
        Ok(())
    }

    /// Check if a file_version can be created
    ///
    /// Add custom business rules that must be satisfied before creation.
    pub fn can_create(&self, entity: &FileVersion) -> Result<bool> {
        self.validate(entity)?;
        Ok(true)
    }

    /// Check if a file_version can be updated
    pub fn can_update(&self, _current: &FileVersion, _updated: &FileVersion) -> Result<bool> {
        // Add update permission/business logic here
        Ok(true)
    }

    /// Check if a file_version can be deleted
    pub fn can_delete(&self, _entity: &FileVersion) -> Result<bool> {
        // Add deletion permission/business logic here
        Ok(true)
    }

    // =========================================================================
    // Custom Business Operations
    // =========================================================================
    // <<< CUSTOM OPERATIONS START >>>

    // <<< CUSTOM OPERATIONS START >>>
    // Add your custom domain operations here
    // Example:
    // pub fn calculate_something(&self, entity: &FileVersion) -> Result<i32> {
    //     // Business logic here
    //     Ok(0)
    // }
    // <<< CUSTOM OPERATIONS END >>>
}

impl Default for FileVersionDomainService {
    fn default() -> Self {
        Self::new()
    }
}