//! Domain policy for ProcessingJob aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::ProcessingJob;

/// Domain policy for ProcessingJob — enforces business invariants.
pub struct ProcessingJobDomainPolicy;

#[async_trait]
impl DomainPolicy<ProcessingJob> for ProcessingJobDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &ProcessingJob) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked processing_job cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}