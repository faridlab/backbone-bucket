//! Domain policy for ConversionJob aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::ConversionJob;

/// Domain policy for ConversionJob — enforces business invariants.
pub struct ConversionJobDomainPolicy;

#[async_trait]
impl DomainPolicy<ConversionJob> for ConversionJobDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &ConversionJob) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked conversion_job cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}