//! Domain policy for UploadSession aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::UploadSession;

/// Domain policy for UploadSession — enforces business invariants.
pub struct UploadSessionDomainPolicy;

#[async_trait]
impl DomainPolicy<UploadSession> for UploadSessionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &UploadSession) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked upload_session cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}