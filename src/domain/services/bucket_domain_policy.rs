//! Domain policy for Bucket aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Bucket;

/// Domain policy for Bucket — enforces business invariants.
pub struct BucketDomainPolicy;

#[async_trait]
impl DomainPolicy<Bucket> for BucketDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Bucket) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked bucket cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}