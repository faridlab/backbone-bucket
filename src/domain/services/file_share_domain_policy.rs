//! Domain policy for FileShare aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::FileShare;

/// Domain policy for FileShare — enforces business invariants.
pub struct FileShareDomainPolicy;

#[async_trait]
impl DomainPolicy<FileShare> for FileShareDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &FileShare) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked file_share cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}