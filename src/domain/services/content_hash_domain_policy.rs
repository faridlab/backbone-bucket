//! Domain policy for ContentHash aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use backbone_core::PermitAllPolicy;
use crate::domain::entity::ContentHash;

/// Domain policy for ContentHash — permits all operations (no business invariants).
///
/// To add domain rules, replace this alias with a struct implementing
/// `backbone_core::DomainPolicy<ContentHash>` in the `// <<< CUSTOM` zone.
pub type ContentHashDomainPolicy = PermitAllPolicy<ContentHash>;

// <<< CUSTOM
// END CUSTOM
