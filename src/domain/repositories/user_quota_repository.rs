//! Repository trait for UserQuota aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UserQuota aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{UserQuota, QuotaStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type UserQuotaPaginationParams = DomainPaginationParams;
pub type UserQuotaPaginatedResult = DomainPaginatedResult<UserQuota>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserQuotaFilter {
    pub user_id: Option<Uuid>,
    pub limit_bytes: Option<i64>,
    pub used_bytes: Option<i64>,
    pub max_file_size: Option<i64>,
    pub tier: Option<String>,
    pub quota_status: Option<QuotaStatus>,
    pub peak_usage_bytes: Option<i64>,
}

impl UserQuotaFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.limit_bytes.is_some() || self.used_bytes.is_some() || self.max_file_size.is_some() || self.tier.is_some() || self.quota_status.is_some() || self.peak_usage_bytes.is_some()
    }
}

/// Repository trait for UserQuota aggregate
///
/// This trait defines the persistence contract for UserQuota entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserQuotaRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user_quota entity
    async fn save(&self, entity: &UserQuota) -> Result<UserQuota>;

    /// Find user_quota by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UserQuota>>;

    /// Find all user_quota entities
    async fn find_all(&self) -> Result<Vec<UserQuota>>;

    /// Update user_quota by ID
    async fn update(&self, id: &str, entity: &UserQuota) -> Result<Option<UserQuota>>;

    /// Delete user_quota by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user_quota with pagination
    async fn list(&self, params: UserQuotaPaginationParams) -> Result<UserQuotaPaginatedResult>;

    /// List user_quota with pagination and filters
    async fn list_with_filters(&self, params: UserQuotaPaginationParams, filters: UserQuotaFilter) -> Result<UserQuotaPaginatedResult>;

    /// Count all user_quota entities
    async fn count(&self) -> Result<u64>;

    /// Count user_quota entities matching filters
    async fn count_with_filters(&self, filters: UserQuotaFilter) -> Result<u64>;

    /// Check if user_quota exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user_quota by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user_quota by ID
    async fn restore(&self, id: &str) -> Result<Option<UserQuota>>;

    /// List soft-deleted user_quota entities
    async fn list_deleted(&self, params: UserQuotaPaginationParams) -> Result<UserQuotaPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find user_quota by user_id
    async fn find_by_user_id(&self, user_id: Uuid) -> Result<Option<UserQuota>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if user_quota exists by user_id
    async fn exists_by_user_id(&self, user_id: Uuid) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user_quota entities
    async fn bulk_save(&self, entities: &[UserQuota]) -> Result<Vec<UserQuota>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
