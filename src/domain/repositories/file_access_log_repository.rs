//! Repository trait for FileAccessLog aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the FileAccessLog aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{FileAccessLog, AccessAction};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type FileAccessLogPaginationParams = DomainPaginationParams;
pub type FileAccessLogPaginatedResult = DomainPaginatedResult<FileAccessLog>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct FileAccessLogFilter {
    pub file_id: Option<Uuid>,
    pub user_id: Option<Uuid>,
    pub share_id: Option<Uuid>,
    pub action: Option<AccessAction>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
}

impl FileAccessLogFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.file_id.is_some() || self.user_id.is_some() || self.share_id.is_some() || self.action.is_some() || self.ip_address.is_some() || self.user_agent.is_some()
    }
}

/// Repository trait for FileAccessLog aggregate
///
/// This trait defines the persistence contract for FileAccessLog entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait FileAccessLogRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new file_access_log entity
    async fn save(&self, entity: &FileAccessLog) -> Result<FileAccessLog>;

    /// Find file_access_log by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<FileAccessLog>>;

    /// Find all file_access_log entities
    async fn find_all(&self) -> Result<Vec<FileAccessLog>>;

    /// Update file_access_log by ID
    async fn update(&self, id: &str, entity: &FileAccessLog) -> Result<Option<FileAccessLog>>;

    /// Delete file_access_log by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List file_access_log with pagination
    async fn list(&self, params: FileAccessLogPaginationParams) -> Result<FileAccessLogPaginatedResult>;

    /// List file_access_log with pagination and filters
    async fn list_with_filters(&self, params: FileAccessLogPaginationParams, filters: FileAccessLogFilter) -> Result<FileAccessLogPaginatedResult>;

    /// Count all file_access_log entities
    async fn count(&self) -> Result<u64>;

    /// Count file_access_log entities matching filters
    async fn count_with_filters(&self, filters: FileAccessLogFilter) -> Result<u64>;

    /// Check if file_access_log exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save file_access_log entities
    async fn bulk_save(&self, entities: &[FileAccessLog]) -> Result<Vec<FileAccessLog>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
