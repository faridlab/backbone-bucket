//! Repository trait for StoredFile aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StoredFile aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StoredFile, FileStatus, ProcessingStatus, ThreatLevel};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StoredFilePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StoredFilePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StoredFilePaginatedResult {
    pub data: Vec<StoredFile>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StoredFileFilter {
    pub bucket_id: Option<Uuid>,
    pub owner_id: Option<Uuid>,
    pub path: Option<String>,
    pub original_name: Option<String>,
    pub size_bytes: Option<i64>,
    pub mime_type: Option<String>,
    pub checksum: Option<String>,
    pub is_compressed: Option<bool>,
    pub original_size: Option<i64>,
    pub compression_algorithm: Option<String>,
    pub is_scanned: Option<bool>,
    pub threat_level: Option<ThreatLevel>,
    pub has_thumbnail: Option<bool>,
    pub thumbnail_path: Option<String>,
    pub has_video_thumbnail: Option<bool>,
    pub has_document_preview: Option<bool>,
    pub processing_status: Option<ProcessingStatus>,
    pub content_hash_id: Option<Uuid>,
    pub cdn_url: Option<String>,
    pub owner_module: Option<String>,
    pub owner_entity: Option<String>,
    pub owner_entity_id: Option<Uuid>,
    pub field_name: Option<String>,
    pub status: Option<FileStatus>,
    pub storage_key: Option<String>,
    pub previous_version_id: Option<Uuid>,
}

impl StoredFileFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.bucket_id.is_some() || self.owner_id.is_some() || self.path.is_some() || self.original_name.is_some() || self.size_bytes.is_some() || self.mime_type.is_some() || self.checksum.is_some() || self.is_compressed.is_some() || self.original_size.is_some() || self.compression_algorithm.is_some() || self.is_scanned.is_some() || self.threat_level.is_some() || self.has_thumbnail.is_some() || self.thumbnail_path.is_some() || self.has_video_thumbnail.is_some() || self.has_document_preview.is_some() || self.processing_status.is_some() || self.content_hash_id.is_some() || self.cdn_url.is_some() || self.owner_module.is_some() || self.owner_entity.is_some() || self.owner_entity_id.is_some() || self.field_name.is_some() || self.status.is_some() || self.storage_key.is_some() || self.previous_version_id.is_some()
    }
}

/// Repository trait for StoredFile aggregate
///
/// This trait defines the persistence contract for StoredFile entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StoredFileRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stored_file entity
    async fn save(&self, entity: &StoredFile) -> Result<StoredFile>;

    /// Find stored_file by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StoredFile>>;

    /// Find all stored_file entities
    async fn find_all(&self) -> Result<Vec<StoredFile>>;

    /// Update stored_file by ID
    async fn update(&self, id: &str, entity: &StoredFile) -> Result<Option<StoredFile>>;

    /// Delete stored_file by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stored_file with pagination
    async fn list(&self, params: StoredFilePaginationParams) -> Result<StoredFilePaginatedResult>;

    /// List stored_file with pagination and filters
    async fn list_with_filters(&self, params: StoredFilePaginationParams, filters: StoredFileFilter) -> Result<StoredFilePaginatedResult>;

    /// Count all stored_file entities
    async fn count(&self) -> Result<u64>;

    /// Count stored_file entities matching filters
    async fn count_with_filters(&self, filters: StoredFileFilter) -> Result<u64>;

    /// Check if stored_file exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stored_file by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stored_file by ID
    async fn restore(&self, id: &str) -> Result<Option<StoredFile>>;

    /// List soft-deleted stored_file entities
    async fn list_deleted(&self, params: StoredFilePaginationParams) -> Result<StoredFilePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stored_file entities
    async fn bulk_save(&self, entities: &[StoredFile]) -> Result<Vec<StoredFile>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
