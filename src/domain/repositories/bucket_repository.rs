//! Repository trait for Bucket aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Bucket aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Bucket, BucketStatus, BucketType, StorageBackend};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BucketPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl BucketPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct BucketPaginatedResult {
    pub data: Vec<Bucket>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BucketFilter {
    pub name: Option<String>,
    pub slug: Option<String>,
    pub description: Option<String>,
    pub owner_id: Option<Uuid>,
    pub bucket_type: Option<BucketType>,
    pub status: Option<BucketStatus>,
    pub storage_backend: Option<StorageBackend>,
    pub root_path: Option<String>,
    pub total_size_bytes: Option<i64>,
    pub max_file_size: Option<i64>,
    pub enable_cdn: Option<bool>,
    pub enable_versioning: Option<bool>,
    pub enable_deduplication: Option<bool>,
}

impl BucketFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.slug.is_some() || self.description.is_some() || self.owner_id.is_some() || self.bucket_type.is_some() || self.status.is_some() || self.storage_backend.is_some() || self.root_path.is_some() || self.total_size_bytes.is_some() || self.max_file_size.is_some() || self.enable_cdn.is_some() || self.enable_versioning.is_some() || self.enable_deduplication.is_some()
    }
}

/// Repository trait for Bucket aggregate
///
/// This trait defines the persistence contract for Bucket entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BucketRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new bucket entity
    async fn save(&self, entity: &Bucket) -> Result<Bucket>;

    /// Find bucket by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Bucket>>;

    /// Find all bucket entities
    async fn find_all(&self) -> Result<Vec<Bucket>>;

    /// Update bucket by ID
    async fn update(&self, id: &str, entity: &Bucket) -> Result<Option<Bucket>>;

    /// Delete bucket by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List bucket with pagination
    async fn list(&self, params: BucketPaginationParams) -> Result<BucketPaginatedResult>;

    /// List bucket with pagination and filters
    async fn list_with_filters(&self, params: BucketPaginationParams, filters: BucketFilter) -> Result<BucketPaginatedResult>;

    /// Count all bucket entities
    async fn count(&self) -> Result<u64>;

    /// Count bucket entities matching filters
    async fn count_with_filters(&self, filters: BucketFilter) -> Result<u64>;

    /// Check if bucket exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete bucket by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted bucket by ID
    async fn restore(&self, id: &str) -> Result<Option<Bucket>>;

    /// List soft-deleted bucket entities
    async fn list_deleted(&self, params: BucketPaginationParams) -> Result<BucketPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find bucket by slug
    async fn find_by_slug(&self, slug: &str) -> Result<Option<Bucket>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if bucket exists by slug
    async fn exists_by_slug(&self, slug: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save bucket entities
    async fn bulk_save(&self, entities: &[Bucket]) -> Result<Vec<Bucket>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
