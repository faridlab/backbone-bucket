//! Repository trait for UploadSession aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UploadSession aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{UploadSession, StorageBackend, UploadStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UploadSessionPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl UploadSessionPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct UploadSessionPaginatedResult {
    pub data: Vec<UploadSession>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UploadSessionFilter {
    pub bucket_id: Option<Uuid>,
    pub user_id: Option<Uuid>,
    pub path: Option<String>,
    pub filename: Option<String>,
    pub mime_type: Option<String>,
    pub file_size: Option<i64>,
    pub status: Option<UploadStatus>,
    pub storage_backend: Option<StorageBackend>,
}

impl UploadSessionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.bucket_id.is_some() || self.user_id.is_some() || self.path.is_some() || self.filename.is_some() || self.mime_type.is_some() || self.file_size.is_some() || self.status.is_some() || self.storage_backend.is_some()
    }
}

/// Repository trait for UploadSession aggregate
///
/// This trait defines the persistence contract for UploadSession entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UploadSessionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new upload_session entity
    async fn save(&self, entity: &UploadSession) -> Result<UploadSession>;

    /// Find upload_session by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UploadSession>>;

    /// Find all upload_session entities
    async fn find_all(&self) -> Result<Vec<UploadSession>>;

    /// Update upload_session by ID
    async fn update(&self, id: &str, entity: &UploadSession) -> Result<Option<UploadSession>>;

    /// Delete upload_session by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List upload_session with pagination
    async fn list(&self, params: UploadSessionPaginationParams) -> Result<UploadSessionPaginatedResult>;

    /// List upload_session with pagination and filters
    async fn list_with_filters(&self, params: UploadSessionPaginationParams, filters: UploadSessionFilter) -> Result<UploadSessionPaginatedResult>;

    /// Count all upload_session entities
    async fn count(&self) -> Result<u64>;

    /// Count upload_session entities matching filters
    async fn count_with_filters(&self, filters: UploadSessionFilter) -> Result<u64>;

    /// Check if upload_session exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete upload_session by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted upload_session by ID
    async fn restore(&self, id: &str) -> Result<Option<UploadSession>>;

    /// List soft-deleted upload_session entities
    async fn list_deleted(&self, params: UploadSessionPaginationParams) -> Result<UploadSessionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save upload_session entities
    async fn bulk_save(&self, entities: &[UploadSession]) -> Result<Vec<UploadSession>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
