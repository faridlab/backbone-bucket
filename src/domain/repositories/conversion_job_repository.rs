//! Repository trait for ConversionJob aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ConversionJob aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ConversionJob, ConversionStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type ConversionJobPaginationParams = DomainPaginationParams;
pub type ConversionJobPaginatedResult = DomainPaginatedResult<ConversionJob>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ConversionJobFilter {
    pub source_file_id: Option<Uuid>,
    pub target_format: Option<String>,
    pub status: Option<ConversionStatus>,
    pub result_file_id: Option<Uuid>,
    pub error_message: Option<String>,
}

impl ConversionJobFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.source_file_id.is_some() || self.target_format.is_some() || self.status.is_some() || self.result_file_id.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for ConversionJob aggregate
///
/// This trait defines the persistence contract for ConversionJob entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ConversionJobRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new conversion_job entity
    async fn save(&self, entity: &ConversionJob) -> Result<ConversionJob>;

    /// Find conversion_job by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ConversionJob>>;

    /// Find all conversion_job entities
    async fn find_all(&self) -> Result<Vec<ConversionJob>>;

    /// Update conversion_job by ID
    async fn update(&self, id: &str, entity: &ConversionJob) -> Result<Option<ConversionJob>>;

    /// Delete conversion_job by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List conversion_job with pagination
    async fn list(&self, params: ConversionJobPaginationParams) -> Result<ConversionJobPaginatedResult>;

    /// List conversion_job with pagination and filters
    async fn list_with_filters(&self, params: ConversionJobPaginationParams, filters: ConversionJobFilter) -> Result<ConversionJobPaginatedResult>;

    /// Count all conversion_job entities
    async fn count(&self) -> Result<u64>;

    /// Count conversion_job entities matching filters
    async fn count_with_filters(&self, filters: ConversionJobFilter) -> Result<u64>;

    /// Check if conversion_job exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete conversion_job by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted conversion_job by ID
    async fn restore(&self, id: &str) -> Result<Option<ConversionJob>>;

    /// List soft-deleted conversion_job entities
    async fn list_deleted(&self, params: ConversionJobPaginationParams) -> Result<ConversionJobPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save conversion_job entities
    async fn bulk_save(&self, entities: &[ConversionJob]) -> Result<Vec<ConversionJob>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
