//! Domain events for ContentHash entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ContentHash;

/// Domain events for ContentHash — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ContentHash>)` variant.
pub type ContentHashEvent = CrudEvent<ContentHash>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ContentHash events.
pub type ContentHashEventPublisher = Arc<dyn CrudEventPublisher<ContentHash> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopContentHashEventPublisher = NoOpCrudEventPublisher<ContentHash>;
