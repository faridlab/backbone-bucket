//! Domain events for UserQuota entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::UserQuota;

/// Domain events for UserQuota — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<UserQuota>)` variant.
pub type UserQuotaEvent = CrudEvent<UserQuota>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for UserQuota events.
pub type UserQuotaEventPublisher = Arc<dyn CrudEventPublisher<UserQuota> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopUserQuotaEventPublisher = NoOpCrudEventPublisher<UserQuota>;
