//! Domain events for StoredFile entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StoredFile;

/// Domain events for StoredFile — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StoredFile>)` variant.
pub type StoredFileEvent = CrudEvent<StoredFile>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StoredFile events.
pub type StoredFileEventPublisher = Arc<dyn CrudEventPublisher<StoredFile> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStoredFileEventPublisher = NoOpCrudEventPublisher<StoredFile>;
