//! Domain events for FileShare entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::FileShare;

/// Domain events for FileShare — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<FileShare>)` variant.
pub type FileShareEvent = CrudEvent<FileShare>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for FileShare events.
pub type FileShareEventPublisher = Arc<dyn CrudEventPublisher<FileShare> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopFileShareEventPublisher = NoOpCrudEventPublisher<FileShare>;
