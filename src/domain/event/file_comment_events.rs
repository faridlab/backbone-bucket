//! Domain events for FileComment entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::FileComment;

/// Domain events for FileComment — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<FileComment>)` variant.
pub type FileCommentEvent = CrudEvent<FileComment>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for FileComment events.
pub type FileCommentEventPublisher = Arc<dyn CrudEventPublisher<FileComment> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopFileCommentEventPublisher = NoOpCrudEventPublisher<FileComment>;
