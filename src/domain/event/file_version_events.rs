//! Domain events for FileVersion entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::FileVersion;

/// Domain events for FileVersion — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<FileVersion>)` variant.
pub type FileVersionEvent = CrudEvent<FileVersion>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for FileVersion events.
pub type FileVersionEventPublisher = Arc<dyn CrudEventPublisher<FileVersion> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopFileVersionEventPublisher = NoOpCrudEventPublisher<FileVersion>;
