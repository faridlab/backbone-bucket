//! Domain events for ConversionJob entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ConversionJob;

/// Domain events for ConversionJob — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ConversionJob>)` variant.
pub type ConversionJobEvent = CrudEvent<ConversionJob>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ConversionJob events.
pub type ConversionJobEventPublisher = Arc<dyn CrudEventPublisher<ConversionJob> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopConversionJobEventPublisher = NoOpCrudEventPublisher<ConversionJob>;
