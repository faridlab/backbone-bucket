//! Domain events for FileLock entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::FileLock;

/// Domain events for FileLock — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<FileLock>)` variant.
pub type FileLockEvent = CrudEvent<FileLock>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for FileLock events.
pub type FileLockEventPublisher = Arc<dyn CrudEventPublisher<FileLock> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopFileLockEventPublisher = NoOpCrudEventPublisher<FileLock>;
