//! Domain events for Thumbnail entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Thumbnail;

/// Domain events for Thumbnail — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Thumbnail>)` variant.
pub type ThumbnailEvent = CrudEvent<Thumbnail>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Thumbnail events.
pub type ThumbnailEventPublisher = Arc<dyn CrudEventPublisher<Thumbnail> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopThumbnailEventPublisher = NoOpCrudEventPublisher<Thumbnail>;
