//! ConversionJob seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial ConversionJob data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// CONVERSIONJOB SEEDER
// ============================================================================

/// Seeder for conversion_job
pub struct SeedConversionJobSeeder;

impl SeedConversionJobSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedConversionJobSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedConversionJobSeeder {
    fn name(&self) -> &'static str {
        "SeedConversionJobSeeder"
    }

    fn order(&self) -> i32 {
        4
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if conversion_jobs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM conversion_jobs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/conversion_job_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM conversion_jobs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

