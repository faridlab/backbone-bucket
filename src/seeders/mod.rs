//! Database seeders for the bucket module
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This module provides seed data for initial database setup.
//! Seeders can be run via the seeder binary or CLI.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

mod access_log_seeder;
mod bucket_seeder;
mod content_hash_seeder;
mod conversion_job_seeder;
mod file_comment_seeder;
mod file_lock_seeder;
mod file_share_seeder;
mod file_version_seeder;
mod processing_job_seeder;
mod stored_file_seeder;
mod thumbnail_seeder;
mod upload_session_seeder;
mod user_quota_seeder;

pub use access_log_seeder::SeedAccessLogSeeder;
pub use bucket_seeder::SeedBucketSeeder;
pub use content_hash_seeder::SeedContentHashSeeder;
pub use conversion_job_seeder::SeedConversionJobSeeder;
pub use file_comment_seeder::SeedFileCommentSeeder;
pub use file_lock_seeder::SeedFileLockSeeder;
pub use file_share_seeder::SeedFileShareSeeder;
pub use file_version_seeder::SeedFileVersionSeeder;
pub use processing_job_seeder::SeedProcessingJobSeeder;
pub use stored_file_seeder::SeedStoredFileSeeder;
pub use thumbnail_seeder::SeedThumbnailSeeder;
pub use upload_session_seeder::SeedUploadSessionSeeder;
pub use user_quota_seeder::SeedUserQuotaSeeder;

// ============================================================================
// SEEDER TRAIT
// ============================================================================

/// Trait for database seeders
#[async_trait]
pub trait Seeder {
    /// Get the name of this seeder
    fn name(&self) -> &'static str;

    /// Get the order in which this seeder should run
    fn order(&self) -> i32;

    /// Check if this seeder should run (e.g., check if data already exists)
    async fn should_run(&self, pool: &PgPool) -> Result<bool>;

    /// Run the seeder
    async fn run(&self, pool: &PgPool) -> Result<()>;

    /// Rollback the seeder (optional)
    async fn rollback(&self, _pool: &PgPool) -> Result<()> {
        Ok(())
    }
}

// ============================================================================
// SEEDER TYPE
// ============================================================================

/// Type of seeder
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SeederType {
    /// Reference data (required for application to function)
    Reference,
    /// Initial production data
    Production,
    /// Development/testing data
    Development,
    /// Test data (for automated tests)
    Test,
}

// ============================================================================
// CUSTOM SEEDERS
// ============================================================================

// <<< CUSTOM SEEDERS START >>>
// Add custom seeders here
// <<< CUSTOM SEEDERS END >>>

