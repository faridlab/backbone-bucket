//! FileAccessLog seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial FileAccessLog data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// FILEACCESSLOG SEEDER
// ============================================================================

/// Seeder for file_access_log
pub struct SeedFileAccessLogSeeder;

impl SeedFileAccessLogSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedFileAccessLogSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedFileAccessLogSeeder {
    fn name(&self) -> &'static str {
        "SeedFileAccessLogSeeder"
    }

    fn order(&self) -> i32 {
        10
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if file_access_logs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM file_access_logs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/file_access_log_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM file_access_logs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

