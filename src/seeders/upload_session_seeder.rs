//! UploadSession seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial UploadSession data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// UPLOADSESSION SEEDER
// ============================================================================

/// Seeder for upload_session
pub struct SeedUploadSessionSeeder;

impl SeedUploadSessionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedUploadSessionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedUploadSessionSeeder {
    fn name(&self) -> &'static str {
        "SeedUploadSessionSeeder"
    }

    fn order(&self) -> i32 {
        11
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if upload_sessions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM upload_sessions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/upload_session_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM upload_sessions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

